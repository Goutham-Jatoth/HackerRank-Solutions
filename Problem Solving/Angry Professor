public static String angryProfessor(int k, List<Integer> a) {
    int onTime = 0;
    for (int arrival : a) {
        if (arrival <= 0) {
            onTime++;
        }
    }
    return onTime >= k ? "NO" : "YES";
}
